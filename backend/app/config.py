class Config():
    """
    Configuration class that contains all the necessary settings for the application.
    
    This class holds configurations for database URI, AWS credentials, 
    email settings, and other application-related configurations. Sensitive 
    information such as access keys and database credentials should be handled 
    securely through environment variables to prevent exposure.
    """
